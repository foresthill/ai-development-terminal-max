{
  "name": "ai-dev-terminal",
  "private": true,
  "version": "0.1.0",
  "description": "Native macOS terminal multiplexer for running many Claude Code agents in parallel — overview grid, Z-axis depth, git-worktree isolation, golden-spiral macro view.",
  "license": "MIT",
  "author": "foresthill",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.7.1",
    "@tauri-apps/plugin-opener": "^2",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-webgl": "^0.19.0",
    "@xterm/xterm": "^6.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
